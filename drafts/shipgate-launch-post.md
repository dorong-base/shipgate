You can verify every number in this post with one command.

It needed a permit to exist. Here is the permit.

I let AI write marketing for me. It writes fast, it sounds confident, and I stopped trusting it, because confidence is not evidence. So I built shipgate: content either shows its proof, or it does not go out.

Before reaching LinkedIn, this post went through 6 gates [P1]: a complete brief, a filled brand voice, a reviewer score of 9 or higher [P2], a source for every number, an adversarial GO, and a card that stays open until a real measurement lands on it.

Those tags, [P1] [P2]? Left in on purpose. Each one points to a proof point with a named source in the brief this post was written from. A number without a tag gets the whole draft REFUSED [P4].

The command: 20 proofs on a fresh clone, zero API keys [P3], zero dependencies [P6].

No proof. No post.

Repo, live console, and full credits in the first comment.
